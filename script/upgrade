#!/usr/bin/env bash
#
# Run all dotfiles upgraders.

set -e

cd "$(dirname $0)"/..

# find the upgraders and run them iteratively
find -s . -name upgrade.sh | while read upgrader ; do sh -c "${upgrader}" ; done
